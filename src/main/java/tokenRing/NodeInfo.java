package tokenRing;

class NodeInfo {
    String id;
    String host;
    int port;

    public NodeInfo(String id, String host, int port) {
        this.id = id;
        this.host = host;
        this.port = port;
    }
}